//
// Created by Riton on 2019/9/12.
//

/*
 * 题目：
 * 输入个格式“k 字符串”，求字符串中第k个只出现一次的字符，用[]包裹输出；
 * 如果没有解，输出“Myon~”
 * */


#include <iostream>
#include <vector>
#include <string>

using namespace std;

vector<char> show_once(const string &str) {
    auto result = vector<char>();
    auto *count = new int[str.length()];
    for (int i = 0; i < str.length(); ++i) {
        char ch = str[i];
    }

}

int main() {

}
